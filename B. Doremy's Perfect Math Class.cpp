

#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t,n,m,a,b,c;
    cin>>t;
    while (t--)
    {
        int y,z;
        string s,x;
        cin>>n;
        int arr[n+1];
        set<int>ss;
        for(int i=1; i<=n; i++)
        {
            cin >> a;
		ss.insert(a);
        }    for(int i=1; i<=n; i++)
        {

		ss.insert(a);
        }

    }
    cout<<endl;
    //sort(arr+1,arr+n);
    //if() cout<<"YES"<<endl;
    //else cout<<"NO"<<endl;
}
